# Leave blank to use Vite's proxy (see vite.config.ts) — matches local dev with the FastAPI backend on :8000.
# Set to a full URL when the API is deployed elsewhere, e.g. VITE_API_URL=https://api.example.com
VITE_API_URL=
